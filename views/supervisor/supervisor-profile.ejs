<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Supervisor Profile | eCommerce Store</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles/supervisor/supervisor-profile.css">
    <link rel="stylesheet" href="/styles/supervisor/home.css">
</head>
<body>
    <!-- Header -->
    <header class="portal-header">
        <div class="logo">
            <a href="/supervisor" class="header-link">
                <img class="logoi" src="/images/header/logo1.png" alt="Logo">
            </a>
        </div>
        <div class="user-info">
            <span>Welcome, <%= supervisor.first_name %> <%= supervisor.last_name %></span>
            <a href="/supervisor/profile" class="profile-icon active"><i class="fas fa-user-cog"></i></a>
        </div>
    </header>

    <!-- Main Container -->
    <div class="portal-container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <nav class="sidebar-nav">
                <ul>
                    <li><a href="/supervisor"><i class="fas fa-house"></i> Home</a></li>
                    <li><a href="/supervisor/verify-listings"><i class="fas fa-clipboard-check"></i> Verify Product Listings</a></li>
                    <li><a href="/supervisor/manage-inventory"><i class="fas fa-boxes"></i> Manage Inventory</a></li>
                    <li><a id="logout-button" class="btn-logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
                </ul>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <div class="page-header">
                <h2>Profile Settings</h2>
                <div class="date-display">
                    <i class="far fa-calendar-alt"></i> <%= new Date().toLocaleDateString('en-US', {weekday: 'long', year: 'numeric', month: 'long', day: 'numeric'}) %>
                </div>
            </div>

            <!-- Profile Details -->
            <section class="profile-section">
                <h3><i class="fas fa-user"></i> Personal Information</h3>
                <div class="profile-details">
                    <div class="detail-item">
                        <label>User ID:</label>
                        <span><%= supervisor.user_id %></span>
                    </div>
                    <div class="detail-item">
                        <label>First Name:</label>
                        <span><%= supervisor.first_name %></span>
                    </div>
                    <div class="detail-item">
                        <label>Last Name:</label>
                        <span><%= supervisor.last_name %></span>
                    </div>
                    <div class="detail-item">
                        <label>Email:</label>
                        <span><%= supervisor.email %></span>
                    </div>
                    <div class="detail-item">
                        <label>Phone:</label>
                        <span><%= supervisor.phone %></span>
                    </div>
                    <div class="detail-item">
                        <label>Username:</label>
                        <span><%= supervisor.username %></span>
                    </div>
                    <div class="detail-item">
                        <label>Role:</label>
                        <span>Supervisor</span>
                    </div>
                    <div class="detail-item">
                        <label>Joined:</label>
                        <span><%= new Date(supervisor.created_at).toLocaleDateString('en-US', {year: 'numeric', month: 'long', day: 'numeric'}) %></span>
                    </div>
                </div>
            </section>

            <!-- Change Password -->
            <section class="password-section">
                <h3><i class="fas fa-lock"></i> Change Password</h3>
                <form id="change-password-form" class="password-form">
                    <div class="form-group">
                        <label for="current-password">Current Password:</label>
                        <input type="password" id="current-password" name="currentPassword" required>
                    </div>
                    <div class="form-group">
                        <label for="new-password">New Password:</label>
                        <input type="password" id="new-password" name="newPassword" required>
                    </div>
                    <div class="form-group">
                        <label for="confirm-password">Confirm New Password:</label>
                        <input type="password" id="confirm-password" name="confirmPassword" required>
                    </div>
                    <button type="submit" class="btn-submit"><i class="fas fa-save"></i> Update Password</button>
                </form>
                <div id="password-message" class="message"></div>
            </section>
        </main>
    </div>

    <!-- Footer -->
    <footer class="portal-footer">
        <p>&copy; 2025 eCommerce Store. All rights reserved. | <a href="mailto:support@ecommerce.com"><i class="far fa-envelope"></i> Contact Support</a></p>
    </footer>

    <script src="/scripts/supervisor/supervisor-profile.js"></script>
</body>
</html>